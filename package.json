{
  "name": "mern-user-management-system",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "author": {
    "name": "Murtaza Vadnagarwala",
    "email": "vadnagarwalamurtaza@gmail.com",
    "url": "https://github.com/murtazavadnagar"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "test": "npm run test --workspace backend && npm run test --workspace frontend",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  },
  "lint-staged": {
    "backend/src/**/*.{ts,tsx}": "npm run lint --workspace backend",
    "frontend/src/**/*.{ts,tsx}": "npm run lint --workspace frontend"
  }
}
